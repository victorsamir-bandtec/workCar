import { messages as portuguese } from "./pt";
import { messages as english } from "./en";
import { messages as france } from "./fr";

const messages = {
  ...portuguese,
  ...english,
  ...france,
};

export { messages };
